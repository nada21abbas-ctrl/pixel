# pixel
Pixel AI is a deep learning project that focuses on image processing at the pixel level. It uses a Convolutional Neural Network (CNN) to convert RGB images into grayscale images. The project includes image preprocessing, model training, and visual comparison of results.
